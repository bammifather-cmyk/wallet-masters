name: Build Wallet Masters APK

on:
  push:
    branches: [main]
    paths: ['android-app/**', '.github/workflows/build-apk.yml']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.7'

      - name: Decode signing keystore
        run: |
          if [ -n "${{ secrets.WM_KEYSTORE_B64 }}" ]; then
            mkdir -p android-app/keystore
            echo "${{ secrets.WM_KEYSTORE_B64 }}" | base64 -d > android-app/keystore/walletmasters.keystore
            echo "SIGNING=yes" >> $GITHUB_ENV
          else
            echo "SIGNING=no" >> $GITHUB_ENV
          fi

      - name: Build release APK
        working-directory: android-app
        run: |
          if [ "$SIGNING" = "yes" ]; then
            gradle assembleRelease \
              -PWM_STORE_FILE=keystore/walletmasters.keystore \
              -PWM_STORE_PASSWORD="${{ secrets.WM_KS_PASSWORD }}" \
              -PWM_KEY_ALIAS="${{ secrets.WM_KEY_ALIAS }}" \
              -PWM_KEY_PASSWORD="${{ secrets.WM_KEY_PASSWORD }}" \
              --no-daemon
          else
            gradle assembleRelease --no-daemon
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: wallet-masters-apk
          path: android-app/app/build/outputs/apk/release/app-release.apk
          retention-days: 90

      - name: Attach APK to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Wallet Masters App
          files: android-app/app/build/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
